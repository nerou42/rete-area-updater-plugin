{
  "name": "rete-area-updater-plugin",
  "version": "0.1.0",
  "description": "An plugin for the rete area plugin which updates nodes after inputs/outputs/controls have been changed dynamically",
  "scripts": {
    "build": "tsup ./src/index.ts --dts --format esm",
    "lint": "rete lint"
  },
  "author": "Timo Lehnertz",
  "license": "MIT",
  "peerDependencies": {
    "rete": "^2.0.0"
  },
  "devDependencies": {
    "rete-cli": "^1.0.3",
    "tsup": "^8.2.3",
    "typescript": "4.8.4"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "dependencies": {
    "rete-area-plugin": "^2.0.4"
  }
}
